# Setup

**Created by School of AI**

Core:
- Python 3.12+
- Git
- shell/terminal

Core utilities and tests run without Docker or Kubernetes.

Optional:
- Docker / Docker Compose
- Kubernetes local cluster or managed cluster
- Terraform
- Prometheus
- Grafana

Run core validation:

```bash
python -m unittest discover -s tests -v
```

---

**Created by School of AI**
