# Final Exam Answer Key

**Created by School of AI**

1. Frequently integrating code changes with automated validation.

2. Keeping changes in a releasable state through automated delivery stages.

3. Automatically releasing validated changes to production.

4. To promote the same tested bytes through environments.

5. A digest used to verify artifact integrity.

6. A packaged filesystem/runtime definition used to create containers.

7. A controller managing replicated application Pods and rollouts.

8. A stable network abstraction for reaching Pods.

9. Whether a workload should receive traffic.

10. Whether a workload should be restarted because it is unhealthy.

11. Terraform's record mapping configuration to managed infrastructure objects.

12. A difference between declared and actual infrastructure.

13. Granting only the permissions required for the task.

14. A measured indicator of service behavior.

15. A target for an SLI over a defined period.

16. The amount of unreliability allowed by an SLO.

17. A monitoring system and time-series database focused on metrics.

18. A visualization/observability platform for dashboards, exploration and alerting.

19. A Software Bill of Materials listing software components/dependencies.

20. Releasing a change to a small portion of traffic/capacity first.

21. Restoring a known-good version after a bad change.

22. A structured review of an incident to learn and improve systems.

23. Building internal products and golden paths that enable developer self-service.

24. To connect logs/events belonging to the same request or transaction.

25. To reduce unexpected breaking or supply-chain changes.

26. Frequently integrating code changes with automated validation.

27. Keeping changes in a releasable state through automated delivery stages.

28. Automatically releasing validated changes to production.

29. To promote the same tested bytes through environments.

30. A digest used to verify artifact integrity.

31. A packaged filesystem/runtime definition used to create containers.

32. A controller managing replicated application Pods and rollouts.

33. A stable network abstraction for reaching Pods.

34. Whether a workload should receive traffic.

35. Whether a workload should be restarted because it is unhealthy.

36. Terraform's record mapping configuration to managed infrastructure objects.

37. A difference between declared and actual infrastructure.

38. Granting only the permissions required for the task.

39. A measured indicator of service behavior.

40. A target for an SLI over a defined period.

41. The amount of unreliability allowed by an SLO.

42. A monitoring system and time-series database focused on metrics.

43. A visualization/observability platform for dashboards, exploration and alerting.

44. A Software Bill of Materials listing software components/dependencies.

45. Releasing a change to a small portion of traffic/capacity first.

46. Restoring a known-good version after a bad change.

47. A structured review of an incident to learn and improve systems.

48. Building internal products and golden paths that enable developer self-service.

49. To connect logs/events belonging to the same request or transaction.

50. To reduce unexpected breaking or supply-chain changes.

51. Frequently integrating code changes with automated validation.

52. Keeping changes in a releasable state through automated delivery stages.

53. Automatically releasing validated changes to production.

54. To promote the same tested bytes through environments.

55. A digest used to verify artifact integrity.

56. A packaged filesystem/runtime definition used to create containers.

57. A controller managing replicated application Pods and rollouts.

58. A stable network abstraction for reaching Pods.

59. Whether a workload should receive traffic.

60. Whether a workload should be restarted because it is unhealthy.